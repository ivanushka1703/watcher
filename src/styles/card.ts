import colors from './colors';

const card = {
  backgroundColor: colors.tertiaryBackgronud,
  shadowColor: colors.shadow,
  shadowOffset: {
    width: 0,
    height: 1,
  },
  shadowOpacity: 0.2,
  shadowRadius: 1.41,
  elevation: 1.5,
};

export default card;
